# 6. Уравнение регрессии

Регрессия показывает зависимость одной переменной от другой.

Общий вид:

$$Y=f(X)+ε$$

Для линейной модели:

$$Y=a+bX+ε$$

где:

- a — свободный член;
- b — коэффициент регрессии;
- ε — случайная ошибка.